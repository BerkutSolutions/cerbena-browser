{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for desktop launcher.",
  "windows": ["main", "panic-frame-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
